import styled from "styled-components";

const Wrapper = styled.main`
  .snap-message {
    height: 60px;
  }

  .remaining {
    margin-top: 1rem;
  }

  .error {
    color: red;
  }
`;

export default Wrapper;
